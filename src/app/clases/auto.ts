export interface Auto {
    noSerie:string;
    marca:string;
    modelo:string;
    color:string;
    costo:number;
}

//Las API se consumen en formato JSON,
//para crear el modelo, primero hay que saber como se compone el JSON
//que se vebuelve
